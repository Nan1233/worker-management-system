const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');const read=p=>fs.readFileSync(path.join(root,p),'utf8');
test('frontend navigation and routes are permission aware',()=>{assert.match(read('src/layouts/ManagementLayout.tsx'),/item\.roles\.includes\(role\) && can\(item\.permission\)/);assert.match(read('src/layouts/WorkerLayout.tsx'),/can\(item\.permission\)/);assert.match(read('src/routes/AppRouter.tsx'),/PermissionRoute/);});
test('admin has a role and permission management screen',()=>{const page=read('src/pages/admin/Permissions.tsx');assert.match(page,/Vai trò & quyền/);assert.match(page,/permissions\/roles/);assert.match(page,/permissions\/users/);});
test('consolidated professional foundation protects tables and mobile layouts',()=>{const css=read('src/styles/ktc-professional.css')+read('src/pages/manager/Reports.css');assert.match(css,/overflow:\s*auto/);assert.match(css,/prefers-reduced-motion/);assert.match(css,/max-width:\s*700px/);});

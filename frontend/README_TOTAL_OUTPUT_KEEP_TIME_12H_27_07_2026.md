# Tổng sản lượng OK + NG

- Giữ nguyên toàn bộ giao diện và logic thời gian của bản giới hạn 12 giờ.
- Chỉ bổ sung trường chỉ đọc `Tổng sản lượng (OK + NG)`.
- Giá trị tự động bằng `TT OK + TT NG`.
- Không thay đổi payload hoặc cấu trúc dữ liệu backend.

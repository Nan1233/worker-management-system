# KTC P1 Cleanup — 2026-08-20

Implemented:
- Added `backend/package-lock.json` bootstrap matching backend package.json direct dependencies.
- Mapped Tailwind semantic tokens to the canonical KTC `--ktc-*` token system.
- Added shared UI primitives: KtcButton, KtcCard, KtcBadge, KtcField, KtcPageHeader.
- Added frontend visual regression/source contract tests covering critical management/worker surfaces, tokens, primitives, and touch targets.

Validation:
- `node --test frontend/tests/visual-regression.contract.test.cjs` — PASS (4/4).

Before the first backend release from a local clone, run:
`cd backend && npm install --package-lock-only && npm ci`
to materialize the complete dependency graph in the lockfile.

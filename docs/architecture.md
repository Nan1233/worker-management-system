# Kiến trúc KTC Production Control

Nguồn chuẩn gồm `backend`, `frontend` và `desktop`. Mã Electron chỉ tồn tại trong `desktop/electron`. Quyền công đoạn lấy từ `worker_processes`; cấu hình `PROCESS_SELECTIONS` chỉ phục vụ hiển thị giao diện.

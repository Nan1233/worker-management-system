# Sửa hiển thị mã công nhân

- Công nhân hiển thị `worker_code` thay vì username nội bộ.
- Tài khoản gần đây lưu mã 599 để đăng nhập lại, không lưu P599.
- Phiên cũ có thể phục hồi `worker_code` từ JWT mới sau refresh/login.

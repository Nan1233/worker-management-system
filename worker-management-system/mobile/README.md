# KTC Mobile

KTC supports:

- Android via Capacitor (`frontend/android`)
- iOS via Capacitor (`npm run ios:add` on macOS/Xcode)
- Existing iOS `.mobileconfig` / profile delivery under `mobile/ios`

Both native apps use the same Render backend and TiDB Cloud production database.
